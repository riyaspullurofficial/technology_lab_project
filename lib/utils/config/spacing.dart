

import 'package:flutter/material.dart';

Widget textFieldHeightSpacing = SizedBox(
  height: 20,
);

const screenMarginHorizontal = EdgeInsets.symmetric(horizontal: 20);
const screenMarginVertical = EdgeInsets.symmetric(vertical: 10);

const screenMarginHorizontalVertical = EdgeInsets.symmetric(vertical: 10,horizontal: 20);

const homeScreenItemTopSpacing=10;